# Your Anthropic API key from https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Port to run the server on (default: 3000)
PORT=3000

# Optional persistent storage directory for messages.json
# Useful in production so deploys don't overwrite saved messages.
DATA_DIR=./data
